<div class="container mt-4">
    <div class="card shadow-lg">
        <div class="card-header bg-primary text-white">
            <h2 class="text-center">Student List</h2>
        </div>
        <input type="text" id="searchInput" class="form-control mb-3" placeholder="Search students...">
        <div class="card-body">
            <table class="table table-striped table-hover" id="studentTable">
                <thead class="table-dark">
                    <tr>
                        <th>ID</th>
                        <th>Name</th>
                        <th>Surname</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.ID</td>
                            <td>@item.Name</td>
                            <td>@item.Surname</td>
                            <td>
                                <a asp-controller="Student" asp-action="Detail" asp-route-id="@item.ID" class="btn btn-info">Details</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
            <div class="text-center">
                <a asp-action="Create" class="btn btn-success">Add New Student</a>
            </div>
        </div>
    </div>
</div>
